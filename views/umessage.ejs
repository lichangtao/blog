<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="shortcut icon" href="/images/favicon.ico" />
    <title>用户信息</title>
    <style>
        .content{
            margin: 0 auto;
        }
        div{
            margin-top:30px;
        }
    </style>
</head>
<body>
    <div class="content">
        <div>用户：
            <%if( data[0]['nickname']){%> <%= data[0]['nickname']%> <%}else{%>
              <%= data[0]['username']%>
            <%}%>
        </div>
        <div>
            他的文章：
            <ul>
                <%for(var i in data){%>
                    <li>
                        标题：<a href="/article/<%= data[i]['id']%>.html" target="_blank"><%= data[i]['title']%></a>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                        发表时间：<%= data[i]['time']%>
                    </li>
                <%}%>
            </ul>
        </div>
        <div>
            他的评论：
            <ul>
                <%for(var i in comm){%>
                <li>
                    在 <%= comm[i]['time']%> 评论了 <a href="/article/<%= comm[i]['pid']%>.html" target="_blank"><%= comm[i]['title']%></a>

                </li>
                <%}%>
            </ul>
        </div>
    </div>
    <a href="/#features">返回首页</a>

</body>
</html>